## Blockers

## Critical

- I want a solution to launch in background and monitor for completion. directio or fixture framework? https://symfony.com/doc/current/components/process.html#running-processes-asynchronously

- Import might be removing items it should not, same file seemed to work in update, but not import.

## Normal

- `directio update` is not a good name for commiting the marked done to the database, come up with better name

- Think about this syntax instead `## Foo <!--directio id="foo" redo="P1D"-->` with optional? end tag
- instance user do such and such

## Backlog

- add "log" command which is date DESC of items completed.

## Notes

